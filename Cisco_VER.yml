---
- name: First Play
  hosts: Tulsa_Test
  gather_facts: False
  connection:  local
  tasks:
   - name: Fist Task
     ios_command:
         commands: show version
   - name: Second Tack
     ios_command:
         commands: show inventory
     register: version
