---
- name: First Play
  hosts: Tulsa_Test
  gather_facts: False
  connection:  local
  tasks:
   - name: Fist Task
     ios_command:
         commands: show version
     register: version
     vars:
         ansible_command_timeout: 60
